In addition to the default output, it should be possible to output the results in a markdown format.

Optionally it would be very useful to use a template for the markdown output.

The template should be able to use variables from the report object.
